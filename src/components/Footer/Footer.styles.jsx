import styled from "styled-components";

export const FooterCont=styled.footer`
    margin:auto 0 24px;
    & p{
        color:#A9A9A9;
        text-align: center;
        font-size: 1.167rem;
        margin-block: 0;
        font-weight: 500;
    }
    & a{
        all:unset;
        cursor: pointer;
        font-weight: 700;
        text-decoration:underline;
    }
`